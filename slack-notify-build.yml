- name: Run tests
  # ... your test step here
# - name: Notify slack success
#   if: success()
#   env:
#     SLACK_BOT_TOKEN: ${{ secrets.SLACK_NOTIFICATIONS_BOT_TOKEN }}
#   uses: voxmedia/github-action-slack-notify-build@v1
#   with:
#     channel: 04-ml-macro-diffusion-indexes
#     status: SUCCESS
#     color: good

- name: Notify slack fail
  if: failure()
  env:
    SLACK_BOT_TOKEN: ${{ secrets.SLACK_NOTIFICATIONS_BOT_TOKEN }}
  uses: voxmedia/github-action-slack-notify-build@v1
  with:
    channel: 04-ml-macro-diffusion-indexes
    status: FAILED
    color: danger
